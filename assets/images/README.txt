Replace this folder with:
- profile.jpg  (your photo, square, 600x600px or larger recommended)
- favicon.png  (32x32px)

Both are referenced by the site:
- profile.jpg → in _config.yml under author_profile.avatar
- favicon.png → in _includes/head.html
